[package]
name = "virtual-switch"
version = "0.1.0"
edition = "2024"
description = "USB/IP virtual switch for testing the boot selector"

[dependencies]
usbip = "0.8.0"
tokio = { version = "1", features = ["full"] }
inquire = "0.7"
log = "0.4"
env_logger = "0.11"
